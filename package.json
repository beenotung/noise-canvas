{
  "scripts": {
    "deploy": "npm run build && surge build https://noise-canvas.surge.sh",
    "format": "prettier --write src",
    "start": "snowpack dev --port 8100",
    "build": "snowpack build",
    "test": "tsc --noEmit",
    "tsc": "tsc -p ."
  },
  "devDependencies": {
    "@types/node": "^17.0.45",
    "prettier": "^2.7.1",
    "snowpack": "^3.8.8",
    "surge": "^0.24.6",
    "ts-node": "^10.9.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.7.4"
  }
}
